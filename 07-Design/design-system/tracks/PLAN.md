# Branding — Plan (Track A home)

> **Status**: Active  |  **Date**: 2026-07-10  |  **Audience**: Shahin

**Goal:** Turn the published Cytognosis Design System (Claude Design v11.2.1) into a production branding repo: a `design-system/` mirror, a versioned **Website Template** (the one contract Track B consumes), the artifact pack (logos/icons done; templates/backgrounds/VS Code theme queued), and a downstream package for Yar/dashboards.

## How this project works
- Execution plan: `TRACK-A_BRANDING-REPO_PLAN.md`; cross-track map: `TRACKS_ORCHESTRATION_AND_RELOCATION_2026-07-10.md`; shared board: `TRACKS-STATUS-BOARD.md`; Claude Design bridge: `CLAUDE_DESIGN_BRIDGE.md`.
- Canonical repo: `_context/branding-repo`; design knowledge: `_context/07-Design`.
- Design assets staged in `design-system-staging/` until the branding-repo recovery clears the **Ali gate** (`ALI-CONVERSATION-BRIEF.md`).

## Gates
- Branding-repo recovery (CytoStyle replaced the old repo at commit `9bf41cb`; recover lost features from git) is gated on the Ali conversation. Everything upstream (Template + artifacts from the published design) can start now.
