# Agent steering (docs repo, home of record)

- This repo is the home of record for all Cytognosis documents; `PROJECT-MAP.md` at root is the project-to-pillar contract; 8 pillars (`00-Inbox` .. `07-Design`); the Obsidian vault mirrors paths 1:1 and holds readable twins (same filename).
- Standard header blockquote on every content doc (Status/Date/Author/Audience/Tags/Variants); Variants links are bidirectional and mandatory for paired docs.
- Spec registry: `04-Engineering/specs/SPEC-REGISTRY.md`; never silently edit Live specs (deltas in `specs/NNN/changes/`).
- Commits: author `Shahin Mohammadi <mohammadi@cytognosis.org>`; this repo stays local (holds sensitive comp/contract content); never push without explicit direction.
- Before mass edits: create a tag or bundle in `Refactor/_safety-archives` conventions; archive, never delete.
